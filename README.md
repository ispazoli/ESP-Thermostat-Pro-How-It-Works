# ESP Thermostat Pro

This repository includes a **GitHub Pages** documentation site in `/docs`.

- Open: **Settings → Pages → Deploy from a branch → main /docs**
- The Pages site is **static demo/documentation** only (no live ESP calls).